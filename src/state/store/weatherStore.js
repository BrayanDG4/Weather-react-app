import { create } from "zustand";
import { shallow } from "zustand/shallow";
import { useFetch } from "../../hooks/useFetch.js";

export const useWheaterStore = create(
  (set) => ({
    
  }),
  shallow
);
